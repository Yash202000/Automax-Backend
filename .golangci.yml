run:
  timeout: 5m

linters:
  enable:
    - errcheck
    - govet
    - staticcheck
    - unused
    - ineffassign
    - gocritic
    - gosimple

linters-settings:
  errcheck:
    check-blank: false
  gocritic:
    enabled-checks:
      - dupImport
      - commentedOutCode
      - unnecessaryDefer

issues:
  exclude-rules:
    - path: _test\.go
      linters:
        - errcheck
  max-issues-per-linter: 50
  max-same-issues: 10
